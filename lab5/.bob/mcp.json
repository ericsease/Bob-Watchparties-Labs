{
  "mcpServers": {
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "description": "General-purpose HTTP fetch MCP — lets Bob retrieve any public URL. Used in this lab to pull the Spring Boot 3.x migration guide live during the assessment act."
    }
  }
}
